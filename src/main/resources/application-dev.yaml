spring:
  datasource:
    url: jdbc:h2:mem:project26;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

archive:
  cors:
    allowed-origins:
      - "http://localhost:5173"
