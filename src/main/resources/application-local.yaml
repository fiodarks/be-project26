spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/project26
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

archive:
  storage:
    public-base-url: "http://localhost:8080/api/v1"
  cors:
    allowed-origins:
      - "http://localhost:5173"
    allowed-origin-patterns:
      - "http://localhost:*"
      - "http://127.0.0.1:*"
